---
title: Quick Start
description: Get up and running with bbkt in minutes.
---

import { Steps, Code, Tabs, TabItem } from '@astrojs/starlight/components';

This guide will help you set up `bbkt` and start using it with your AI assistant to manage your Bitbucket workspace.

## Prerequisites

- A scoped Atlassian API token (or an OAuth consumer). See [Configuration](/bbkt/getting-started/configuration) for the scoped-vs-classic gotcha.
- An MCP-compatible AI client (Claude Desktop, Cursor, etc.) if you want to use the MCP server.

## Setup

<Steps>

1. **Install bbkt**

   <Tabs>
     <TabItem label="Install Script (Recommended)">
       ```bash
       curl -sSL https://raw.githubusercontent.com/zach-snell/bbkt/main/install.sh | bash
       # or, no sudo:
       curl -sSL https://raw.githubusercontent.com/zach-snell/bbkt/main/install.sh | bash -s -- --user
       ```
     </TabItem>
     <TabItem label="From Source">
       ```bash
       git clone https://github.com/zach-snell/bbkt.git
       cd bbkt
       go build -o bbkt ./cmd/bbkt
       ```
     </TabItem>
   </Tabs>

2. **Authenticate**

   ```bash
   bbkt auth          # API token (default)
   bbkt status        # verify
   ```

3. **Configure your MCP client**

   Add `bbkt` to your Claude Desktop config (`~/Library/Application Support/Claude/claude_desktop_config.json`). Because credentials are stored on disk by step 2, no env block is needed:

   ```json
   {
     "mcpServers": {
       "bitbucket": {
         "command": "bbkt",
         "args": ["mcp"]
       }
     }
   }
   ```

4. **Restart your AI client**

   Close and reopen Claude Desktop (or your MCP client) to load the new server.

5. **Start chatting!**

   You can now ask your AI assistant to interact with your Bitbucket repositories.

</Steps>

## First Commands to Try

Once configured, try asking your AI assistant:

### View your repositories

> "Show me my repositories in the 'acme-corp' workspace"

> "List all open pull requests in the 'frontend-app' repo"

### Manage code and reviews

> "Approve the latest pull request on 'backend-api'"

> "Comment on line 42 of PR #12 saying 'This logic looks fragile'"

### Trigger pipelines

> "Run the deployment pipeline for the `main` branch"

> "Check the status of the failing pipeline run"

## Example Conversation

Here's a typical interaction using the multiplexed tools:

**You**: "Create a new branch called 'feature/new-login' in the 'auth-service' repo"

**Assistant**: *Uses the `manage_refs` tool with action="create-branch"*

> Branch `feature/new-login` has been created from `main`.

**You**: "Can you open a PR for this new branch into main with the title 'Add OAuth Support'?"

**Assistant**: *Uses the `manage_pull_requests` tool with action="create"*

> Pull request #45 has been created: **Add OAuth Support**.
> - Source: `feature/new-login`
> - Destination: `main`

## Next Steps

- [Configuration](/bbkt/getting-started/configuration) — API tokens, OAuth, multi-profile, env vars.
- [MCP Tools Reference](/bbkt/reference/mcp/) — the nine multiplexed MCP tools.
